- name: SonarCloud Scan
  uses: SonarSource/sonarcloud-github-action@v2
  with:
    args: >
      -Dsonar.projectKey=onlinebookreviewsystem_bookreviewfront
      -Dsonar.organization=onlinebookreviewsystem
      -Dsonar.sources=.
  env:
    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
